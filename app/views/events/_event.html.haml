%li.event
  = link_to(render(:partial => 'event_listing', :locals => {:event => event}), event_path(event) )
  / .name=        event.name
  / .description= event.description
  / .date=        event.date

